enum AuthenticateInfo { username, password, isLogin } // for direct entry or login page

enum Themes { theme } // 0 = light, 1 = dark

enum ProfilePages { general, lists, badgets } // for profile screen page view controller enum

enum Gender { male, female, other }
